// Choppa Gear Data
// This file contains all gear sets for the Choppa (Greenskin) class

// Add Choppa gear to the main gearDatabase
if (typeof gearDatabase !== 'undefined' && gearDatabase.recommendations) {
    Object.assign(gearDatabase.recommendations, {
        // Choppa gear sets will be added here
    });
}
