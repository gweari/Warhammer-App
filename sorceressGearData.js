// Sorceress Gear Data
// This file contains all gear sets for the Sorceress (Dark Elf) class

// Add Sorceress gear to the main gearDatabase
if (typeof gearDatabase !== 'undefined' && gearDatabase.recommendations) {
    Object.assign(gearDatabase.recommendations, {
        // Sorceress gear sets will be added here
    });
}
